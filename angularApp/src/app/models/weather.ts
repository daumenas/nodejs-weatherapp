
export abstract class Weather {
  id: number;
  // tslint:disable-next-line:variable-name
  observation_time: Date;
  temp: number;
  // tslint:disable-next-line:variable-name
  weather_code: string;
}
